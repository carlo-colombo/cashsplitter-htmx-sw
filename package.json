{
  "name": "cashsplitter-htmx-sw",
  "version": "1.0.0",
  "description": "CashSplitter is a local-first, offline-capable financial ledger application designed for easy expense tracking and splitting among groups. It's built with a \"Zero-Build Stack,\" emphasizing simplicity and modern web standards.",
  "main": "sw.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npx playwright test --headed --report=list",  
    "test:chrome": "npx playwright test --project=\"Google Chrome\" --reporter=list",
    "test:chrome:headed": "npx playwright test --project=\"Google Chrome\" --headed --report=list",
    "start": "http-server -p 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "http-server": "^14.1.1",
    "wait-on": "^8.0.0"
  }
}
